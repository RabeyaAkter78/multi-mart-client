/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com", "img.freepik.com"], // Add i.pinimg.com to the allowed domains
  },
};

export default nextConfig;
